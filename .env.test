# 测试环境主要给测试团队或联调使用，用来模拟接近真实环境，但允许调试。

# Node.js 运行环境（这里一般仍用 production，或 test，看 CI/CD 怎么配置）
NODE_ENV=production

# 应用运行环境标识（自定义）
VITE_APP_ENV=test

# 测试后端 API 地址（例如：测试服务器 / 内网地址）
VITE_API_BASE_URL=https://test-api.yourdomain.com

# 测试 WebSocket 地址（用 wss，如果后端支持 TLS；如果不支持可以用 ws）
VITE_WS_BASE_URL=wss://test-api.yourdomain.com

# 测试 SSE 地址（通常和 API 一样）
VITE_SSE_BASE_URL=https://test-api.yourdomain.com

# 测试环境 CDN 地址（如果测试也接了 CDN，没有就先用正式或空）
VITE_CDN_PREFIX=https://test-cdn.yourdomain.com